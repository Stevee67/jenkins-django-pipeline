node('docker') {
    stage 'Checkout'
        checkout scm
    stage 'Build & UnitTest'
        sh "echo 'Star unittests'"
        sh "docker-compose run --rm admin-panel bash -c 'cd admin_panel && py.test -v'"
}