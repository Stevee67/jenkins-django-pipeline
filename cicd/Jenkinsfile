node('docker') {
    stage 'Checkout'
        checkout scm
    stage 'Build & UnitTest'
        sh "echo 'HHEHHEH'"
        sh "ls -l"
        sh "ls -l app/"
        sh "docker build -t admin-panel:B${BUILD_NUMBER} -f app/Dockerfile ."
        sh "dd run --rm admin-panel test"
}